<%= form_with(model: number, local: true) do |form| %>
  <% if number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(number.errors.count, "error") %> prohibited this number from being saved:</h2>

      <ul>
      <% number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number, class: "numbers" %>
    <%= form.text_field :number, class:"numbers" %>
  </div>

  <div class="field">
    <%= form.label :number2, class: "numbers" %>
    <%= form.text_field :number2, class:"numbers2" %>
  </div>

  <div class="field">
    <%= form.label :number3, class: "numbers" %>
    <%= form.text_field :number3, class:"numbers3" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<script type="text/javascript">
//Metas: 
// 1) Obtener el valor ingresado y verificar si es múltiplo de $100.000,
// luego al clickear afuera o poner submit, poblar el campo con el múltiplo correcto más cercano, ejemplo: 187.053 -> 200.000
//
//
//
  $(document).ready(function() {
    // code for number field
    $(".numbers").keydown(function (e) {
        // Allow: backspace, delete, tab, escape, enter and .
        if ($.inArray(e.keyCode, [46, 8, 9, 27, 13, 110, 190]) !== -1 ||
             // Allow: Ctrl/cmd+A
            (e.keyCode == 65 && (e.ctrlKey === true || e.metaKey === true)) ||
             // Allow: Ctrl/cmd+C
            (e.keyCode == 67 && (e.ctrlKey === true || e.metaKey === true)) ||
             // Allow: Ctrl/cmd+X
            (e.keyCode == 88 && (e.ctrlKey === true || e.metaKey === true)) ||
             // Allow: home, end, left, right
            (e.keyCode >= 35 && e.keyCode <= 39)) {
                 // let it happen, don't do anything
                 return;
        }
        // Ensure that it is a number and stop the keypress
        if ((e.shiftKey || (e.keyCode < 48 || e.keyCode > 57)) && (e.keyCode < 96 || e.keyCode > 105)) {
            e.preventDefault(alert("solo numeros permitidos"));
            //mi cóodigo
            
            //fin
        }
    });
    // code for number2 field, not allowing . by deleting 190
    $(".numbers2").keydown(function (e) {
        // Allow: backspace, delete, tab, escape, enter and .
        if ($.inArray(e.keyCode, [46, 8, 9, 27, 13, 110]) !== -1 ||
             // Allow: Ctrl/cmd+A
            (e.keyCode == 65 && (e.ctrlKey === true || e.metaKey === true)) ||
             // Allow: Ctrl/cmd+C
            (e.keyCode == 67 && (e.ctrlKey === true || e.metaKey === true)) ||
             // Allow: Ctrl/cmd+X
            (e.keyCode == 88 && (e.ctrlKey === true || e.metaKey === true)) ||
             // Allow: home, end, left, right
            (e.keyCode >= 35 && e.keyCode <= 39)) {
                 // let it happen, don't do anything
                 return;
        }
        // Ensure that it is a number and stop the keypress
        if ((e.shiftKey || (e.keyCode < 48 || e.keyCode > 57)) && (e.keyCode < 96 || e.keyCode > 105)) {
            e.preventDefault(alert("solo numeros permitidos"));
            //mi cóodigo
            
            //fin
        }
    });
    // code for number3 field, onchange form practice
    //Get var bla = $('#txt_name').val();
    //Set $('#txt_name').val(bla);
    $(".numbers3").change(function(){
      alert("texto");
    });

});

  
</script>




